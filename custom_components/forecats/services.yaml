generate_pet_picture:
  name: Generate Pet Picture
  description: >
    Generate a weather-themed pet picture. Provider credentials are configured
    via Settings → Integrations → Forecats.
  fields:
    location:
      description: Location for weather context
      example: "Toronto, Ontario, Canada"
      required: true
      selector:
        text:
    forecast:
      description: Weather forecast data
      required: true
    temperature_unit:
      description: Temperature unit (e.g., "C" or "F")
      required: true
    pets:
      description: "list of your pets, each pet must have a name and description, and can have a type (default: cat)"
      required: true
      selector:
        object:
          label_field: name
          description_field: type
          multiple: true
          fields:
            name:
              label: Name
              selector:
                text:
            type:
              label: Type
              selector:
                text:
            description:
              label: Description
              selector:
                text:
    input_image_paths:
      description: Paths to reference images
      required: true
      selector:
        object:
    art_styles:
      description: List of art styles to choose from
      required: true
      selector:
        object:
    image_gen_aspect_ratio:
      description: Aspect ratio for generated image
      default: "16:9"
      selector:
        text:
    image_gen_resolution:
      description: Resolution for generated image
      default: "1K"
      selector:
        text:
    final_image_size:
      description: Final output image size
      default: "1K"
      example: "800x480"
      selector:
        text:
    display_profile:
      description: Display profile (null or "spectra6")
      required: false
      default: null
      example: "spectra6"
      selector:
        text:
    output_dir:
      description: >
        Optional path to save generated images. When set, images are saved to this directory
        instead of the default "<HA config>/www/daily_forecats/". The directory is created
        if it does not exist.
      required: false
      example: "/config/www/my_forecats"
      selector:
        text:
